package app.defaulty.theme

import androidx.compose.material3.Typography

// Use default Material 3 typography — system font (Roboto).
// Spec: no remote fonts, no custom font loading.
val DefaultyTypography = Typography()
